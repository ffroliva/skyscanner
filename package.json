{
  "private": true,
  "name": "full-stack-recruitment-test",
  "version": "1.0.0",
  "description": "Skyscanner full-stack recruitment test.",
  "scripts": {
    "install": "(cd ./client && npm install) && (cd ./server && mvn install)",
    "client": "(cd ./client && npm start)",
    "build": "(cd ./client && npm run build)",
    "server": "(cd ./server && mvn spring-boot:run)",
    "pretest": "(cd ./client && npm run lint)",
    "test": "(cd ./client && CI=true npm test) && (cd ./server && mvn test)",
    "start": "echo 'Did you mean `mvn spring-boot:run` or `npm run client`?'"
  }
}
